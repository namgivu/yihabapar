config={}

config.update(APP_NAME='YOUR-APP-NAME') #used as container folder in /tmp folder for some tasks
config.update(APP_HOME='path/to/app/home') #the root folder of your application

util_config=config